<UserControl x:Class="Hyakkei.Tool.Translator.TranslatorInfoView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="28,24" MaxWidth="640" HorizontalAlignment="Left">
            <TextBlock Text="翻译" FontSize="26" FontWeight="SemiBold"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
            <TextBlock Text="输入即翻与划词翻译。操作在岛内完成。"
                       FontSize="13" Margin="0,4,0,20"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}" />

            <Border Background="{DynamicResource ControlFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="8" Padding="16,14">
                <StackPanel>
                    <TextBlock FontSize="13" TextWrapping="Wrap" LineHeight="24"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}">
                        <Run Text="面板内输入，停顿或回车即翻译；目标语言分段 自动 / 中 / EN / FR，Ctrl+1-4 切换" />
                        <LineBreak />
                        <Run Text="进入面板挂载热键" /><Run x:Name="HotkeyRun" FontWeight="SemiBold" /><Run Text="：任意应用里有选中文字 → 取词翻译并弹岛；没有选中 → 直接进入截屏翻译，译文就地出卡片" />
                        <LineBreak />
                        <Run Text="岛隐藏后热键依然有效；Esc 或单击 Alt 返回列表即解除" />
                    </TextBlock>
                    <TextBlock x:Name="ProviderText" FontSize="12" Margin="0,10,0,0"
                               Foreground="{DynamicResource TextFillColorTertiaryBrush}" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
